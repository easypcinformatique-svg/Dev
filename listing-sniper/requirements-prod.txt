# Core async
aiohttp>=3.9.0
websockets>=12.0

# Database (optional — tourne sans si pas de PostgreSQL)
asyncpg>=0.29.0
redis>=5.0.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0

# Monitoring
prometheus-client>=0.20.0

# Web dashboard + keep-alive
Flask>=3.0.0
